# 🔐 Настройка авторизации через Telegram

## 📋 Обзор

Система использует **Telegram Login Widget** для безопасной авторизации пользователей. Первый зарегистрированный пользователь автоматически становится администратором, все последующие требуют подтверждения.

---

## 🤖 Шаг 1: Создание Telegram бота

### 1.1 Откройте BotFather

Найдите [@BotFather](https://t.me/BotFather) в Telegram и начните диалог.

### 1.2 Создайте нового бота

```
/newbot
```

Следуйте инструкциям:
1. Введите имя бота (например: `YT Zavod Auth Bot`)
2. Введите username бота (должен заканчиваться на `bot`, например: `yt_zavod_auth_bot`)

### 1.3 Сохраните токен

BotFather выдаст вам токен вида:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

⚠️ **ВАЖНО**: Сохраните этот токен в надежном месте!

### 1.4 Настройте домен для авторизации

Для работы Telegram Login Widget нужно указать домен:

```
/setdomain
```

Выберите вашего бота и укажите домен:
- Для разработки: `http://localhost:5173`
- Для продакшена: `https://yourdomain.com`

⚠️ **Примечание**: В продакшене обязательно используйте HTTPS!

---

## 🔧 Шаг 2: Настройка переменных окружения

### 2.1 Backend (.env в корне проекта)

Создайте или обновите файл `.env`:

```bash
# Security & Authentication
JWT_SECRET=your_very_long_random_secret_key_change_this_in_production_12345

# Telegram Authentication
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BOT_USERNAME=yt_zavod_auth_bot
```

**Генерация JWT_SECRET:**
```powershell
# PowerShell
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 64 | ForEach-Object {[char]$_})
```

### 2.2 Frontend (.env в папке frontend/)

Создайте файл `frontend/.env`:

```bash
# Vite API URL
VITE_API_URL=http://localhost:3000

# Telegram Bot для авторизации
VITE_TELEGRAM_BOT_USERNAME=yt_zavod_auth_bot
```

⚠️ **ВАЖНО**: 
- В продакшене измените `VITE_API_URL` на ваш реальный домен
- Переменные Vite должны начинаться с `VITE_`

---

## 🚀 Шаг 3: Запуск системы

### 3.1 Установите зависимости (если еще не сделали)

```powershell
cd backend
npm install

cd ../frontend
npm install
```

### 3.2 Запустите сервера

**Вариант A: Используя скрипты**

```powershell
# Терминал 1 - Backend
.\start-backend.ps1

# Терминал 2 - Frontend
.\start-frontend.ps1
```

**Вариант B: Вручную**

```powershell
# Терминал 1 - Backend
cd backend
npm run dev

# Терминал 2 - Frontend
cd frontend
npm run dev
```

---

## 👤 Шаг 4: Первый вход (администратор)

1. Откройте браузер: `http://localhost:5173`
2. Вы увидите страницу авторизации через Telegram
3. Нажмите кнопку **"Login with Telegram"**
4. Подтвердите авторизацию в Telegram
5. ✅ **Первый пользователь автоматически становится администратором!**

---

## 👥 Шаг 5: Добавление новых пользователей

### 5.1 Регистрация нового пользователя

1. Новый пользователь заходит на сайт
2. Авторизуется через Telegram
3. Получает сообщение: **"Ожидание подтверждения администратором"**

### 5.2 Подтверждение администратором

**Администратор:**

1. Переходит в раздел **"Пользователи"** (👥 в боковом меню)
2. Видит список ожидающих подтверждения
3. Нажимает **"Подтвердить"** для одобрения пользователя
4. Пользователь получает доступ к системе

**Или отклонить:**
- Нажать **"Отклонить"** для удаления пользователя

---

## 🔐 Безопасность

### Что реализовано:

✅ **Проверка подписи Telegram** - Все данные от Telegram проверяются криптографически

✅ **JWT токены** - Сессии пользователей защищены JWT с истечением через 30 дней

✅ **Rate Limiting** - Ограничение на 10 попыток авторизации за 15 минут

✅ **Подтверждение администратором** - Новые пользователи не могут войти без одобрения

✅ **Роли пользователей** - Разделение прав `admin` и `user`

✅ **Защищенные маршруты** - API эндпоинты проверяют авторизацию и права

### Рекомендации для продакшена:

⚠️ **Обязательно измените:**
- `JWT_SECRET` - используйте длинную случайную строку (64+ символа)
- `TELEGRAM_BOT_TOKEN` - не публикуйте его в репозитории
- Добавьте `.env` в `.gitignore`

⚠️ **Используйте HTTPS:**
- Telegram Login Widget требует HTTPS в продакшене
- Настройте SSL сертификат (Let's Encrypt)

⚠️ **Защитите базу данных:**
- SQLite файл храните вне веб-директории
- Регулярно делайте бэкапы

---

## 📊 Структура базы данных

### Таблица `users`

```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  telegram_id INTEGER UNIQUE NOT NULL,
  username TEXT,
  first_name TEXT,
  last_name TEXT,
  photo_url TEXT,
  role TEXT DEFAULT 'user' CHECK(role IN ('admin', 'user')),
  is_approved INTEGER DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🔌 API Endpoints

### Авторизация

**POST** `/api/auth/telegram`
- Авторизация через Telegram
- Body: данные от Telegram Widget
- Response: `{ user, token, requiresApproval }`

**GET** `/api/auth/me`
- Информация о текущем пользователе
- Headers: `Authorization: Bearer <token>`

### Управление пользователями (только админ)

**GET** `/api/auth/users`
- Список всех пользователей

**GET** `/api/auth/pending-users`
- Пользователи, ожидающие подтверждения

**POST** `/api/auth/approve/:id`
- Подтвердить пользователя

**POST** `/api/auth/reject/:id`
- Отклонить пользователя

**POST** `/api/auth/change-role/:id`
- Изменить роль пользователя
- Body: `{ role: 'admin' | 'user' }`

**GET** `/api/auth/stats`
- Статистика пользователей

---

## 🎨 Frontend компоненты

### Созданные страницы:

- `LoginPage.jsx` - Страница авторизации через Telegram
- `UsersManagementPage.jsx` - Управление пользователями (админ)
- `ProtectedRoute.jsx` - Компонент защиты маршрутов

### Обновленные компоненты:

- `App.jsx` - Добавлена система маршрутизации с защитой
- `Sidebar.jsx` - Добавлен пункт "Пользователи" для админов и информация о пользователе

### Store:

- `authStore.js` - Zustand store для управления авторизацией

---

## 🐛 Устранение проблем

### Ошибка: "TELEGRAM_BOT_TOKEN не настроен"

**Решение:** Проверьте файл `.env` в корне проекта, убедитесь что токен правильный.

### Ошибка: "Данные не прошли проверку"

**Причины:**
- Неверный `TELEGRAM_BOT_TOKEN`
- Данные старше 24 часов
- Подделка данных

**Решение:** Перезапустите backend с правильным токеном.

### Telegram Login Widget не отображается

**Причины:**
- Неверный `VITE_TELEGRAM_BOT_USERNAME`
- Не настроен домен в BotFather

**Решение:**
1. Проверьте `frontend/.env`
2. Выполните `/setdomain` в BotFather

### JWT токен не работает

**Решение:**
1. Очистите localStorage в браузере
2. Перезайдите через Telegram
3. Проверьте `JWT_SECRET` в `.env`

---

## ✅ Проверка работоспособности

### 1. Backend запущен

```
✅ YT Zavod Backend
📡 Сервер запущен: http://localhost:3000
```

### 2. Frontend запущен

```
VITE ready in 500 ms
➜ Local: http://localhost:5173/
```

### 3. База данных создана

Должен появиться файл `backend/data/trends.db` с таблицей `users`.

### 4. Авторизация работает

1. Откройте `http://localhost:5173`
2. Должна отобразиться страница логина
3. Кнопка Telegram должна быть видна
4. После клика - редирект в Telegram для подтверждения

---

## 📝 Дополнительные команды

### Проверить статус авторизации

```powershell
# Получить информацию о пользователе
curl http://localhost:3000/api/auth/me `
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

### Посмотреть всех пользователей (админ)

```powershell
curl http://localhost:3000/api/auth/users `
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

---

## 🎉 Готово!

Теперь ваша система защищена авторизацией через Telegram с подтверждением администратором!

**Следующие шаги:**
1. ✅ Создайте бота через BotFather
2. ✅ Настройте переменные окружения
3. ✅ Запустите backend и frontend
4. ✅ Зарегистрируйтесь как первый администратор
5. ✅ Подтверждайте новых пользователей

---

## 📞 Поддержка

Если возникли вопросы или проблемы - проверьте:
- Логи backend (терминал с `npm run dev`)
- Консоль браузера (F12 -> Console)
- Файл `.env` (правильные токены)
- BotFather (настройка домена)

**Удачи! 🚀**
