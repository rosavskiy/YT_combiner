# ✅ Интеграция Whisper AI - Завершена

**Дата:** 14 декабря 2025  
**Статус:** 🟢 Готово к использованию

---

## 🎯 Что было реализовано

### 1. Передача пользовательских OpenAI ключей

**Файл:** `backend/src/services/videoDownloadService.js`

✅ Добавлен импорт `UserSettingsSQLite`  
✅ При запуске парсинга извлекается `openai_api_key` из настроек пользователя  
✅ Ключ передаётся в Python worker через переменную окружения `OPENAI_API_KEY`  
✅ Работает как в Redis режиме, так и в inline режиме  
✅ Логирование: `[Parser] Используем пользовательский OpenAI ключ для userId: X`

**Код:**

```javascript
// Получить пользовательский OpenAI ключ из настроек
const customEnv = {};
if (userId) {
  try {
    const userOpenAIKey = UserSettingsSQLite.get(userId, 'openai_api_key', '');
    if (userOpenAIKey) {
      customEnv.OPENAI_API_KEY = userOpenAIKey;
      console.log(`[Parser] Используем пользовательский OpenAI ключ для userId: ${userId}`);
    }
  } catch (e) {
    console.warn(`[Parser] Не удалось получить OpenAI ключ пользователя: ${e.message}`);
  }
}

const result = await this._runPythonScript('video_parser.py', args, {
  customEnv, // Передача в Python
  onStdout: async (text) => { ... }
});
```

### 2. Поддержка custom environment в _runPythonScript

**Обновлённая сигнатура:**

```javascript
_runPythonScript(scriptName, args = [], { onStdout, onStderr, customEnv = {} } = {})
```

**Передача в spawn:**

```javascript
env: { ...process.env, ...customEnv, PYTHONIOENCODING: 'utf-8' }
```

Теперь каждый Python скрипт получает:
- Все глобальные env переменные из backend
- Пользовательские переменные (customEnv)
- PYTHONIOENCODING для корректного отображения UTF-8

### 3. Добавлена колонка "Полный текст" в Google Sheets

**Файл:** `python-workers/video_parser.py`

**Изменения в схеме таблицы:**

```python
def _default_sheet_headers(self):
    return [[
        'Video ID',            # A
        'URL',                 # B
        'Название',            # C
        'Канал',               # D
        'Дата публикации',     # E
        'Длительность (ЧЧ:ММ:СС)',  # F
        'Таймкоды (список)',   # G
        'Субтитры',            # H
        'Язык субтитров',      # I
        'Полный текст (первые 500 символов)',  # J ← НОВОЕ!
        'Теги/Категории',      # K
        'Статус',              # L
    ]]
```

**Обработка полного текста:**

```python
full_text = data.get('full_text', '')
# Ограничим полный текст для таблицы (первые 500 символов)
full_text_preview = full_text[:500] + '...' if len(full_text) > 500 else full_text
```

**Вставка в таблицу:**

```python
values = [[
    info['video_id'],       # A
    url,                    # B
    info.get('title', ''),  # C
    info.get('channel', ''),# D
    upload_date,            # E
    duration_hhmmss,        # F
    chapters_multiline,     # G
    has_subs,               # H
    subs_lang,              # I
    full_text_preview,      # J ← Превью расшифровки!
    tags_str,               # K
    status,                 # L
]]
```

---

## 📋 Как это работает (Полный цикл)

### Сценарий 1: Парсинг через UI

```
1. Пользователь нажимает "Парсить" на странице Download
   └─> POST /api/videos/parse
       └─> req.user.id передаётся в videoDownloadService.addParseJob()

2. Backend извлекает настройки пользователя
   └─> UserSettingsSQLite.get(userId, 'openai_api_key')
   └─> Создаётся customEnv = { OPENAI_API_KEY: 'user_key' }

3. Запускается Python worker
   └─> spawn('python', ['video_parser.py', videoId, ...])
   └─> env: { ...process.env, ...customEnv }

4. Python получает переменную окружения
   └─> os.environ.get('OPENAI_API_KEY')
   └─> Используется в _transcribe_via_openai_whisper()

5. Whisper API расшифровывает аудио
   └─> client = OpenAI(api_key=api_key)
   └─> transcript = client.audio.transcriptions.create(...)

6. Результат сохраняется
   ├─> JSON файл: full_text + segments
   └─> Google Sheets: колонка J = первые 500 символов
```

### Сценарий 2: Автопарсинг после скачивания

```
1. Видео скачано успешно
   └─> downloadQueue.on('completed')

2. Проверка AUTO_PARSE_AFTER_DOWNLOAD !== 'false'
   └─> Если true (по умолчанию)

3. Запуск автопарсинга
   └─> videoDownloadService.addParseJob(videoId, {
       userId: job.data.userId,  ← Берётся из скачивания
       autoTriggered: true
   })

4. Далее как в Сценарии 1
```

---

## 🔄 Приоритет получения транскрипта

Python автоматически выбирает лучший источник:

```
1. YouTube субтитры (ручные)
   └─> Если доступны → используем ✅ БЕСПЛАТНО

2. YouTube субтитры (автогенерируемые)
   └─> Если доступны → используем ✅ БЕСПЛАТНО

3. OpenAI Whisper API
   └─> Если есть OPENAI_API_KEY И нет субтитров → используем 💰 ПЛАТНО
   └─> Если ENABLE_ASR_IF_NO_CAPTIONS=0 → пропускаем

4. Нет расшифровки
   └─> full_text = ""
```

**Код в video_parser.py:**

```python
# Сначала пробуем YouTube API
transcript = self.get_transcript(video_id, languages, translate_to=translate_to)

if transcript:
    full_text = self.get_full_text(transcript)
else:
    # Нет субтитров — пробуем Whisper
    use_asr = os.environ.get('ENABLE_ASR_IF_NO_CAPTIONS', '1') not in ('0', 'false', 'no')
    api_key = os.environ.get('OPENAI_API_KEY')
    
    if use_asr and api_key:
        print("[INFO] Субтитров нет — пробуем OpenAI Whisper API")
        asr_data = self._transcribe_via_openai_whisper(video_id, api_key)
        if asr_data:
            transcript = asr_data
            full_text = self.get_full_text(transcript)
```

---

## 📊 Где сохраняется расшифровка?

### 1. Google Sheets (превью)

| Колонка | Название | Пример |
|---------|----------|--------|
| **H** | Субтитры | `да` |
| **I** | Язык субтитров | `en` или `auto` |
| **J** | Полный текст (первые 500 символов) | `Never gonna give you up Never gonna let you down...` |

**Ограничения:**
- Только первые 500 символов (Google Sheets имеет лимит на размер ячейки)
- Для полного текста см. JSON файл

### 2. JSON файл (полный текст)

**Путь:** `python-workers/<VIDEO_ID>_parsed.json`

```json
{
  "info": { ... },
  "chapters": [ ... ],
  "transcript": {
    "language": "en",
    "type": "asr_openai",
    "segments": [
      {
        "start": 0.0,
        "duration": 3.5,
        "text": "Never gonna give you up"
      },
      {
        "start": 3.5,
        "duration": 2.8,
        "text": "Never gonna let you down"
      }
    ],
    "source": "openai_whisper"
  },
  "full_text": "Never gonna give you up Never gonna let you down Never gonna run around and desert you..."
}
```

### 3. SQLite база данных

**Таблица:** `videos`  
**Колонка:** `data` (TEXT) — содержит JSON с полными результатами парсинга

```sql
SELECT data FROM videos WHERE video_id = 'dQw4w9WgXcQ';
```

---

## 🎛️ Настройки и переменные окружения

### Backend (.env в корне):

```bash
# OpenAI ключ (глобальный, если не установлен у пользователя)
OPENAI_API_KEY=sk-proj-xxxxx

# Включить/выключить Whisper если нет субтитров (по умолчанию: 1)
ENABLE_ASR_IF_NO_CAPTIONS=1

# Модель Whisper (только whisper-1 доступна)
ASR_MODEL=whisper-1

# Максимальный размер аудио для ASR (в байтах, по умолчанию 100MB)
ASR_MAX_BYTES=104857600

# Автопарсинг после скачивания (по умолчанию: true)
AUTO_PARSE_AFTER_DOWNLOAD=true
```

### Пользовательские настройки (через UI):

**Страница:** Профиль → API ключи

**Поля:**
- YouTube API Key
- OpenAI API Key ← Whisper
- Google Sheets Spreadsheet ID

**Хранение:** `user_settings` таблица в SQLite  
**Приоритет:** Пользовательский ключ > Глобальный ключ из .env

---

## 📚 Документация для пользователей

### Созданные файлы:

1. **WHISPER_AI_SETUP.md** — Подробное руководство (3000+ слов)
   - Получение OpenAI ключа
   - Настройка в системе
   - Стоимость и лимиты
   - Troubleshooting
   - Примеры использования

2. **WHISPER_QUICK_START.md** — Быстрый старт (5 минут)
   - Кратко 3 шага
   - Визуальная структура
   - FAQ и решение проблем

---

## ✅ Тестирование

### Чек-лист для проверки:

- [ ] OpenAI ключ добавлен в Профиль
- [ ] Backend перезапущен
- [ ] Python пакет `openai` установлен: `pip install openai>=1.40.0`
- [ ] Найдено тестовое видео **без субтитров**
- [ ] Запущен парсинг через UI
- [ ] В логах появилось: `[Parser] Используем пользовательский OpenAI ключ`
- [ ] В логах появилось: `[Python] [INFO] Субтитров нет — пробуем OpenAI Whisper API`
- [ ] В логах появилось: `[Python] [SUCCESS] Транскрибация выполнена: N сегментов`
- [ ] Google Sheets обновлена: колонка J содержит текст
- [ ] JSON файл создан с полным транскриптом

### Команды для тестирования:

```powershell
# 1. Установить зависимости
cd python-workers
pip install openai>=1.40.0

# 2. Перезапустить backend
cd ../backend
npm run dev

# 3. Проверить логи (смотреть в терминале backend)
# Должны появиться строки с [Parser] и [Python]

# 4. Проверить результат в Google Sheets
# Открыть таблицу → искать Video ID → проверить колонку J

# 5. Проверить JSON файл
cd ../python-workers
ls -la *_parsed.json
cat <VIDEO_ID>_parsed.json | grep -A 5 "full_text"
```

---

## 💡 Рекомендации

### Для пользователей:

1. **Используйте персональные ключи** через Профиль
   - Каждый сотрудник свой OpenAI аккаунт
   - Раздельный учёт расходов
   - Независимые лимиты

2. **Мониторьте расходы** на [OpenAI Dashboard](https://platform.openai.com/usage)
   - Установите лимиты расходов
   - Проверяйте баланс раз в неделю

3. **Экономьте деньги**
   - Whisper используется только при отсутствии субтитров
   - Большинство популярных видео имеют субтитры
   - Можно отключить: `ENABLE_ASR_IF_NO_CAPTIONS=0`

### Для администраторов:

1. **Глобальный ключ** (опционально)
   - Добавить в `.env`: `OPENAI_API_KEY=...`
   - Будет использоваться если у пользователя нет своего

2. **Мониторинг логов**
   - Логи содержат информацию о каждом вызове Whisper
   - Можно настроить алерты на превышение лимитов

3. **Обучение пользователей**
   - Отправить ссылку на `WHISPER_QUICK_START.md`
   - Провести демонстрацию на собрании

---

## 🎉 Итог

### Что теперь умеет система:

✅ **Универсальный парсинг** — любое видео, с субтитрами или без  
✅ **99+ языков** — автоматическое определение  
✅ **Временные метки** — точная синхронизация текста и видео  
✅ **Персональные ключи** — каждый пользователь свой OpenAI аккаунт  
✅ **Экономичность** — Whisper только при необходимости  
✅ **Автоматизация** — парсинг после скачивания  
✅ **Google Sheets** — сохранение превью расшифровки  
✅ **JSON** — полный текст с сегментами  
✅ **Логирование** — прозрачность процесса  

---

**Whisper AI интеграция завершена и готова к использованию! 🚀**

**Следующие шаги:**
1. Протестировать на реальных видео
2. Обучить пользователей
3. Мониторить расходы
4. Собрать обратную связь
