# Python Workers - Обработка видео

⚠️ **Этот модуль пока не требуется для работы приложения!**

Основной функционал (получение трендов, интерфейс) работает на Node.js + React.

Python workers понадобятся для:
- Продвинутого скачивания видео (yt-dlp)
- Распознавания речи (Whisper)
- Генерации озвучки (gTTS)
- Обработки и монтажа видео (MoviePy)
- Автоматического перевода (deep-translator)

## 🔧 Установка (опционально)

Если хотите использовать Python функционал:

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте
# Windows PowerShell:
.\venv\Scripts\Activate.ps1

# Установите зависимости
pip install -r requirements.txt

# Запустите worker
python app.py
```

Worker будет доступен на http://localhost:5000

## 📋 Зависимости

- **Flask** - веб-фреймворк
- **yt-dlp** - скачивание видео
- **whisper** - распознавание речи
- **moviepy** - обработка видео
- **gTTS** - генерация голоса
- **deep-translator** - переводы

## ⚡ Текущий статус

🔴 **Не реализовано** - модуль в разработке

Пока используйте:
- Backend (Node.js) для получения трендов
- Frontend (React) для интерфейса

Python workers добавим позже для продвинутой обработки видео.

## 💡 Игнорирование ошибок

Если видите красные подчеркивания в `app.py` - это нормально!
Pylance жалуется на отсутствие Flask. Код обернут в try-except и не сломается.

Для работы приложения Python workers не нужны.
