# 🎙️ Настройка Whisper AI для расшифровки видео

## 📋 Что это такое?

Whisper AI от OpenAI — это система автоматического распознавания речи (ASR), которая может:
- **Транскрибировать аудио** в текст с временными метками
- **Работать на 99+ языках** (включая русский, английский, украинский и др.)
- **Определять язык автоматически** если не указан
- **Создавать субтитры** там, где их нет

---

## ✅ Как это работает в YT Zavod?

### Приоритет получения транскрипта:

1. **YouTube субтитры** (ручные) — если доступны
2. **YouTube субтитры** (автогенерируемые) — если доступны
3. **OpenAI Whisper API** — если есть ключ и субтитров нет
4. **Локальный Whisper** — если установлен (требует мощный ПК)

### Где используется:

- ✅ При парсинге видео через кнопку **"Парсить"** на странице Download
- ✅ При **автоматическом парсинге** после скачивания (если включено)
- ✅ Результат попадает в **Google Sheets** в колонку "Субтитры"

---

## 🔑 Шаг 1: Получение OpenAI API ключа

### 1.1 Создайте аккаунт OpenAI

1. Перейдите на [platform.openai.com/signup](https://platform.openai.com/signup)
2. Зарегистрируйтесь через email или Google
3. Подтвердите email если требуется

### 1.2 Добавьте способ оплаты

⚠️ **Важно:** OpenAI требует привязать карту, даже для пробного использования

1. Откройте [platform.openai.com/account/billing/overview](https://platform.openai.com/account/billing/overview)
2. Нажмите **"Add payment method"**
3. Введите данные карты (работают карты РФ? - проверьте актуальную информацию)
4. Пополните баланс минимум на **$5**

### 1.3 Создайте API ключ

1. Откройте [platform.openai.com/api-keys](https://platform.openai.com/api-keys)
2. Нажмите **"+ Create new secret key"**
3. Введите название: `YT Zavod Whisper`
4. **Скопируйте ключ** (он показывается только один раз!)
   - Формат: `sk-proj-xxxxxxxxxxxxxxxxxxxxx`
5. Сохраните ключ в надёжное место

---

## 💰 Стоимость Whisper API

**Цены на декабрь 2024:**
- **$0.006** за минуту аудио
- Для видео длиной **10 минут**: ~$0.06
- Для видео длиной **60 минут**: ~$0.36
- Пополнения на **$5** хватит на ~**13 часов** аудио

**Экономия:**
- Whisper используется **только если нет субтитров**
- YouTube обычно имеет автосубтитры → бесплатно
- Можно отключить через настройку `ENABLE_ASR_IF_NO_CAPTIONS=0`

---

## 🔧 Шаг 2: Настройка в YT Zavod

### Вариант А: Через Web UI (Рекомендуется для сотрудников)

#### Для обычного пользователя:

1. Войдите в систему через Telegram
2. Откройте страницу **"Профиль"** (иконка пользователя в сайдбаре)
3. Найдите раздел **"API ключи"**
4. Вставьте ваш OpenAI API ключ в поле **"OpenAI API Key"**
5. Нажмите **"Сохранить"**

✅ Готово! Ключ сохранён только для вас и будет использоваться при парсинге ваших видео.

#### Для администратора:

Если вы админ и хотите настроить ключ для другого пользователя:

1. Откройте страницу **"Управление пользователями"**
2. Найдите нужного пользователя
3. Нажмите **"Редактировать ключи"**
4. Вставьте OpenAI API ключ
5. Сохраните

---

### Вариант Б: Через переменные окружения (Для разработчиков/деплоя)

#### Backend (.env в корне проекта):

```bash
# OpenAI для Whisper транскрибации
OPENAI_API_KEY=sk-proj-ваш_ключ_здесь

# Опционально: включить/отключить Whisper если нет субтитров
ENABLE_ASR_IF_NO_CAPTIONS=1

# Опционально: модель Whisper (по умолчанию: whisper-1)
ASR_MODEL=whisper-1

# Опционально: максимальный размер аудио для ASR (в байтах, по умолчанию 100MB)
ASR_MAX_BYTES=104857600
```

#### Python Workers (.env в python-workers/):

Если запускаете Python workers отдельно:

```bash
# OpenAI API ключ
OPENAI_API_KEY=sk-proj-ваш_ключ_здесь
```

**Перезапустите backend:**

```powershell
cd backend
npm run dev
```

---

## 🚀 Шаг 3: Проверка работы

### 3.1 Подготовка тестового видео

Найдите YouTube видео **без субтитров** для проверки:
- Старые видео часто не имеют автосубтитров
- Видео на редких языках
- Частные каналы без субтитров

Пример: https://www.youtube.com/watch?v=dQw4w9WgXcQ

### 3.2 Запуск парсинга

**Через UI:**

1. Откройте страницу **"Скачивание"** (Download)
2. Вставьте Video ID или URL: `dQw4w9WgXcQ`
3. Выберите качество
4. Нажмите **"Скачать"**
5. После завершения скачивания нажмите **"Парсить"**

**Через прямой запуск парсинга:**

1. Откройте страницу **"Скачивание"**
2. В поле Video ID введите: `dQw4w9WgXcQ`
3. Нажмите **"Парсить"** (если видео не скачано - парсер всё равно заработает)

### 3.3 Отслеживание процесса

Откройте **Backend логи** (терминал где запущен backend):

```
[Python] [INFO] Субтитров нет — пробуем OpenAI Whisper API
[Python] [INFO] Скачиваем аудио для транскрибации...
[Python] [INFO] Аудио сохранено: /tmp/tmpXXXX.mp3
[Python] [INFO] Отправляем на транскрибацию в OpenAI...
[Python] [SUCCESS] Транскрибация выполнена: 42 сегментов
```

### 3.4 Проверка результата

#### В Google Sheets:

1. Откройте вашу Google таблицу
2. Найдите строку с Video ID
3. Проверьте колонки:
   - **"Субтитры"**: должно быть `да`
   - **"Язык субтитров"**: `auto` или язык видео
   - Полный текст должен быть в JSON файле

#### В JSON файле:

```bash
cd python-workers
ls -la *_parsed.json
cat <VIDEO_ID>_parsed.json
```

Проверьте структуру:

```json
{
  "info": { ... },
  "chapters": [ ... ],
  "transcript": {
    "language": "en",
    "type": "asr_openai",
    "segments": [
      {
        "start": 0.0,
        "duration": 3.5,
        "text": "Never gonna give you up"
      },
      ...
    ],
    "source": "openai_whisper"
  },
  "full_text": "Never gonna give you up Never gonna let you down..."
}
```

---

## 🎛️ Настройки и опции

### Переменные окружения (Backend .env):

| Переменная | Значение | Описание |
|------------|----------|----------|
| `OPENAI_API_KEY` | `sk-proj-...` | **Обязательно** для Whisper API |
| `ENABLE_ASR_IF_NO_CAPTIONS` | `1` или `0` | Использовать Whisper если нет субтитров (по умолчанию: `1`) |
| `ASR_MODEL` | `whisper-1` | Модель OpenAI (только `whisper-1` доступна) |
| `ASR_MAX_BYTES` | `104857600` | Макс размер аудио (100MB по умолчанию) |
| `AUTO_PARSE_AFTER_DOWNLOAD` | `true` | Автопарсинг после скачивания |

### Параметры в Python скрипте:

```bash
python video_parser.py VIDEO_ID \
  --languages en ru uk \
  --translate-to ru \
  --spreadsheet YOUR_SPREADSHEET_ID \
  --sheet-name "Videos"
```

---

## 🔍 Поиск проблем

### Ошибка: "OPENAI_API_KEY не настроен"

**Причина:** Ключ не передан в Python worker

**Решение:**
1. Проверьте что ключ добавлен в Profile → API ключи
2. Или добавьте в `.env` корневой директории: `OPENAI_API_KEY=sk-proj-...`
3. Перезапустите backend

### Ошибка: "Библиотека openai не установлена"

**Причина:** Отсутствует Python пакет `openai`

**Решение:**

```bash
cd python-workers
pip install openai>=1.40.0
```

### Ошибка: "Не удалось скачать аудио"

**Причина:** Видео приватное, удалено или ограничено регионом

**Решение:**
- Проверьте что видео доступно в браузере
- Используйте cookies.txt для приватных видео
- Попробуйте другое видео

### Ошибка: "Rate limit exceeded"

**Причина:** Превышен лимит запросов OpenAI API

**Решение:**
- Проверьте [Usage limits](https://platform.openai.com/account/limits)
- Пополните баланс
- Подождите сброса лимита (обычно 1 минута)

### Whisper не запускается хотя ключ есть

**Проверьте:**

```bash
# В терминале backend
echo $OPENAI_API_KEY

# Или в PowerShell
$env:OPENAI_API_KEY
```

Если пусто — переменная не загружена. Перезапустите backend.

---

## 📊 Куда попадает текст расшифровки?

### 1. Google Sheets

**Колонки:**
- **Колонка H**: "Субтитры" → `да` или `нет`
- **Колонка I**: "Язык субтитров" → `en`, `ru`, `auto` и т.д.

**Полный текст:**
- Сохраняется в JSON файле, но **НЕ** в таблицу (слишком большой)
- Можно добавить дополнительную колонку если нужно

### 2. JSON файл

Путь: `python-workers/<VIDEO_ID>_parsed.json`

```json
{
  "transcript": {
    "segments": [...],
    "source": "openai_whisper"
  },
  "full_text": "Полный текст всех сегментов..."
}
```

### 3. Backend база данных (SQLite)

В таблице `videos` сохраняется:
- `video_id`
- `title`
- `processed = 1` (флаг что парсинг завершён)
- `data` - JSON с полными данными парсинга

---

## 🆕 Дополнительные возможности

### Принудительное использование Whisper

Даже если есть субтитры, можно **принудительно** использовать Whisper для лучшего качества:

**TODO:** Добавить чекбокс в UI "Использовать Whisper вместо субтитров"

Пока можно через прямой вызов Python:

```bash
cd python-workers
python video_parser.py VIDEO_ID \
  --use-whisper \
  --spreadsheet YOUR_ID
```

### Локальный Whisper (без OpenAI)

Для экономии или offline работы можно установить локальный Whisper:

```bash
pip install openai-whisper
# Требует: ffmpeg, PyTorch (большой размер ~1GB+)
```

В `video_parser.py` автоматически будет использован локальный Whisper если:
- Нет `OPENAI_API_KEY`
- Библиотека `whisper` установлена

**Модели:**
- `tiny` - быстрая, но неточная
- `base` - по умолчанию
- `small` - хороший баланс
- `medium` - высокое качество
- `large` - максимальное качество (медленно)

---

## ✅ Чек-лист готовности

- [ ] OpenAI API ключ получен
- [ ] Баланс пополнен минимум на $5
- [ ] Ключ добавлен в Profile → API ключи
- [ ] Python пакет `openai` установлен (`pip install openai`)
- [ ] Backend перезапущен
- [ ] Тестовое видео без субтитров найдено
- [ ] Парсинг запущен успешно
- [ ] Результат появился в Google Sheets
- [ ] JSON файл создан с транскриптом

---

## 📞 Поддержка

**Логи:**
- Backend: смотрите терминал где запущен `npm run dev`
- Python: выводится в backend логи с префиксом `[Python]`

**Документация:**
- OpenAI Whisper API: https://platform.openai.com/docs/guides/speech-to-text
- Цены: https://openai.com/pricing
- Лимиты: https://platform.openai.com/account/limits

**Проблемы?**
- Проверьте логи backend
- Убедитесь что Python зависимости установлены
- Проверьте баланс OpenAI
- Используйте тестовое видео с известным результатом

---

## 🎯 Итоги

После настройки:

✅ **Автоматическая расшифровка** видео без субтитров  
✅ **99+ языков** поддержки  
✅ **Временные метки** для каждой фразы  
✅ **Сохранение в Google Sheets**  
✅ **Экономия** — используется только когда нет субтитров  
✅ **Персональные ключи** — каждый сотрудник свой  

Whisper AI делает парсинг видео **универсальным** даже для контента без субтитров!
