# ✅ Чек-лист настройки автоматизации

## Подготовка

- [ ] Node.js установлен (v18+)
- [ ] Python установлен (v3.8+)
- [ ] Redis запущен
- [ ] Nginx настроен (для продакшена)

## 1. Telegram бот

- [ ] Создан бот через [@BotFather](https://t.me/BotFather)
- [ ] Получен токен бота
- [ ] Установлены команды бота (`/setcommands`)
- [ ] Токен добавлен в `backend/.env` как `TELEGRAM_BOT_TOKEN`
- [ ] Webhook URL добавлен в `.env` как `TELEGRAM_WEBHOOK_URL`
- [ ] Webhook установлен: `curl -X POST localhost:3000/api/telegram/set-webhook`
- [ ] Webhook проверен: `curl localhost:3000/api/telegram/webhook-info`
- [ ] Бот отвечает на команду `/start`

## 2. API ключи

### YouTube API
- [ ] Создан проект в [Google Cloud Console](https://console.cloud.google.com/)
- [ ] Включен YouTube Data API v3
- [ ] Создан API ключ
- [ ] Ключ добавлен в `backend/.env` как `YOUTUBE_API_KEY`
- [ ] Ключ добавлен через настройки пользователя в веб-интерфейсе

### OpenAI API (для Whisper)
- [ ] Зарегистрирован на [platform.openai.com](https://platform.openai.com/)
- [ ] Создан API ключ
- [ ] Ключ добавлен через настройки пользователя в веб-интерфейсе
- [ ] Или установлен локальный Whisper в `python-workers/`

### Google Sheets API (опционально)
- [ ] Создан Service Account в Google Cloud
- [ ] Скачан JSON с credentials
- [ ] Файл размещен как `python-workers/google-credentials.json`
- [ ] ID таблицы добавлен через настройки пользователя

## 3. Backend

- [ ] Зависимости установлены: `cd backend && npm install`
- [ ] База данных инициализирована (создается автоматически)
- [ ] `.env` файл создан и заполнен
- [ ] Сервер запускается: `npm run dev` (без ошибок)
- [ ] Эндпоинт работает: `curl localhost:3000/api/health`

## 4. Frontend

- [ ] Зависимости установлены: `cd frontend && npm install`
- [ ] Сборка работает: `npm run build`
- [ ] Dev сервер запускается: `npm run dev`
- [ ] Открывается в браузере: `http://localhost:5173`
- [ ] Авторизация через Telegram работает

## 5. Python Workers

- [ ] Создан virtual environment: `python -m venv venv`
- [ ] venv активирован: `source venv/bin/activate` (Linux/Mac) или `venv\Scripts\activate` (Windows)
- [ ] Зависимости установлены: `pip install -r requirements.txt`
- [ ] yt-dlp работает: `yt-dlp --version`
- [ ] Whisper установлен (если используется локально)
- [ ] Flask сервер запускается: `python app.py`

## 6. Автоматизация

### Авто-парсинг после скачивания
- [ ] В `.env` установлено `AUTO_PARSE_AFTER_DOWNLOAD=true`
- [ ] Скачивание видео работает
- [ ] После скачивания автоматически запускается парсинг
- [ ] Результаты сохраняются в базу

### N8N интеграция (опционально)
- [ ] N8N сервер запущен и доступен
- [ ] Webhook URL добавлен в `.env` как `N8N_WEBHOOK_URL`
- [ ] Workflows импортированы из `n8n-workflows/`
- [ ] Credentials настроены в n8n
- [ ] Webhook получает уведомления о парсинге

## 7. Проверка функционала

### Telegram бот
- [ ] Авторизация на сайте работает
- [ ] Бот отвечает на `/start`
- [ ] Команда `/help` показывает список команд
- [ ] `/add_channel <URL>` добавляет канал
- [ ] `/list_channels` показывает список
- [ ] `/remove_channel <ID>` удаляет канал
- [ ] Канал появляется в веб-интерфейсе

### Скачивание и парсинг
- [ ] Скачивание видео через веб-интерфейс работает
- [ ] Видео загружается в `python-workers/downloads/`
- [ ] Парсинг автоматически запускается
- [ ] Транскрипт извлекается (Whisper или YouTube)
- [ ] Результаты видны в интерфейсе
- [ ] N8N получает webhook (если настроен)

### Whisper транскрибация
- [ ] Видео без субтитров транскрибируется
- [ ] OpenAI API работает (если настроен)
- [ ] Локальный Whisper работает (если установлен)
- [ ] Транскрипт сохраняется в JSON

### Google Sheets (опционально)
- [ ] Таблица создана и доступна
- [ ] Service Account имеет доступ к таблице
- [ ] Данные экспортируются в таблицу

## 8. Продакшен (если деплоите)

### Nginx
- [ ] Nginx установлен и запущен
- [ ] Конфигурация добавлена из `nginx.conf`
- [ ] SSL сертификат установлен (Let's Encrypt)
- [ ] Проксирование работает на `/api/`
- [ ] Статика отдается из `frontend/dist/`
- [ ] Nginx перезагружен: `sudo systemctl reload nginx`

### PM2
- [ ] PM2 установлен глобально: `npm install -g pm2`
- [ ] Процессы запущены: `pm2 start ecosystem.config.js`
- [ ] Статус проверен: `pm2 status`
- [ ] Логи доступны: `pm2 logs`
- [ ] Автозапуск настроен: `pm2 startup` и `pm2 save`

### Безопасность
- [ ] `.env` файлы не коммитятся в git
- [ ] JWT_SECRET изменен с дефолтного
- [ ] Firewall настроен (только 80, 443, 22)
- [ ] Redis защищен паролем (если публичный)
- [ ] Регулярные бэкапы базы настроены

## 9. Мониторинг

- [ ] PM2 показывает все процессы зелеными
- [ ] Логи не показывают критических ошибок
- [ ] Redis работает: `redis-cli ping` → `PONG`
- [ ] Webhook Telegram активен (проверить через `/webhook-info`)
- [ ] Диск имеет достаточно места для видео

## Troubleshooting

### Если что-то не работает:

**Бот не отвечает:**
```bash
pm2 logs yt-combiner-backend
curl localhost:3000/api/telegram/webhook-info
```

**Парсинг не запускается:**
```bash
pm2 logs yt-combiner-python
tail -f python-workers/logs/app.log
```

**Видео не скачиваются:**
```bash
cd python-workers
source venv/bin/activate
yt-dlp --version
```

**Redis не работает:**
```bash
sudo systemctl status redis
redis-cli ping
```

---

## 🎉 Готово!

Если все пункты отмечены — система полностью настроена и готова к работе!

Теперь можно:
- ✅ Добавлять каналы через Telegram бота
- ✅ Автоматически парсить новые видео
- ✅ Получать транскрипты через Whisper AI
- ✅ Интегрировать с внешними системами через n8n

**Полезные команды:**

```bash
# Проверка статуса
pm2 status

# Логи
pm2 logs

# Перезапуск
pm2 restart all

# Webhook info
curl localhost:3000/api/telegram/webhook-info

# Здоровье API
curl localhost:3000/api/health
```
