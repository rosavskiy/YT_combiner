# Быстрый старт YT Combiner

## 📋 Требования

- Node.js 18+ и npm
- MongoDB (локально или облачный)
- Redis (опционально, для очередей)
- Python 3.9+ (для обработки видео)

## 🚀 Установка

### 1. Клонируйте и перейдите в проект
```bash
cd d:\Projects\YT_combiner
```

### 2. Настройте .env файл
Откройте файл `.env` и добавьте ключи:

```
# YouTube API (для трендов)
YOUTUBE_API_KEY=ваш_ключ_здесь

# Google Sheets (для записи результатов парсинга)
# Рекомендуется: путь к JSON ключу сервисного аккаунта (файл НЕ хранить в гите)
GOOGLE_CREDENTIALS_PATH=D:\Projects\YT_combiner\python-workers\google-credentials.json

# Альтернативы (любая из них):
# GOOGLE_APPLICATION_CREDENTIALS=D:\path\to\service-account.json
# GOOGLE_CREDENTIALS_JSON={"type":"service_account", ...}     # raw JSON
# GOOGLE_CREDENTIALS_JSON=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50Iiwg...  # base64 JSON
```

### 3. Установите зависимости Backend
```powershell
cd backend
npm install
```

### 4. Установите зависимости Frontend
```powershell
cd ..\frontend
npm install
```

### 5. (Опционально) Python Workers
```powershell
cd ..\python-workers
python -m pip install -r requirements.txt
```

## ▶️ Запуск

### Вариант 1: Ручной запуск (рекомендуется для разработки)

#### Терминал 1 - Backend:
```powershell
cd backend
npm run dev
```

#### Терминал 2 - Frontend:
```powershell
cd frontend
npm run dev
```

#### Терминал 3 - MongoDB (если локально):
```powershell
mongod
```

#### Терминал 4 - Redis (опционально):
```powershell
redis-server
```

### Вариант 2: Быстрый старт без БД (только API)

Если у вас нет MongoDB локально, можно использовать облачный MongoDB Atlas:

1. Зарегистрируйтесь на https://www.mongodb.com/cloud/atlas
2. Создайте бесплатный кластер
3. Получите connection string
4. Обновите `.env`: `MONGODB_URI=ваш_connection_string`

## 🌐 Открыть приложение

После запуска откройте в браузере:
- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3000
- **API Docs**: http://localhost:3000/

## 📝 Первые шаги

1. Откройте **Настройки** в приложении
2. Вставьте ваш YouTube API ключ
3. Перейдите на страницу **Тренды**
4. Нажмите кнопку **"Обновить тренды"**
5. Наблюдайте за загрузкой трендов из 19 стран! 🌍

## 🎯 Структура проекта

```
YT_combiner/
├── .env                    # Конфигурация (API ключи)
├── backend/                # Node.js + Express
│   ├── src/
│   │   ├── config/        # Конфигурация (страны, БД)
│   │   ├── models/        # Mongoose модели
│   │   ├── routes/        # API эндпоинты
│   │   ├── services/      # Бизнес-логика
│   │   └── server.js      # Точка входа
│   └── package.json
├── frontend/               # React + Vite
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── pages/         # Страницы приложения
│   │   ├── services/      # API клиент
│   │   ├── stores/        # Zustand хранилища
│   │   └── App.jsx
│   └── package.json
└── python-workers/         # Python микросервисы (TODO)
    ├── app.py
    └── requirements.txt
```

## 🛠️ Разработка

### Backend
- Nodemon автоматически перезапускает сервер при изменениях
- Логи выводятся в консоль
- WebSocket для real-time обновлений

### Frontend
- Vite HMR (Hot Module Replacement)
- React Query для кэширования данных
- Ant Design для UI компонентов

## ❓ Частые проблемы

### Ошибка подключения к MongoDB
```
Решение: Убедитесь что MongoDB запущен или используйте MongoDB Atlas
```

### API ключ не работает
```
Решение: 
1. Проверьте что API включен в Google Cloud Console
2. Проверьте квоту API (бесплатно 10,000 запросов/день)
3. Убедитесь что ключ правильно скопирован
```

### Порты заняты
```
Решение: Измените порты в .env файле:
PORT=3001 (backend)
Или в vite.config.js для frontend
```

## 📚 API Эндпоинты

### Тренды
- `GET /api/trends/countries` - Список стран
- `POST /api/trends/fetch-all` - Загрузить тренды всех стран
- `GET /api/trends/latest` - Последние тренды
- `GET /api/trends/history` - История загрузок

### Видео
- `POST /api/videos/download` - Скачать видео
- `GET /api/videos/info/:videoId` - Информация о видео
- `GET /api/videos/downloaded` - Список скачанных

### Генератор
- `POST /api/generator/translate` - Генерация с переводом
- `GET /api/generator/languages` - Поддерживаемые языки

## 🎉 Готово!

Теперь вы можете:
- ✅ Отслеживать тренды в 19 странах
- ✅ Получать real-time обновления
- ✅ Анализировать популярные видео
- 🔜 Скачивать видео (в разработке)
- 🔜 Генерировать контент (в разработке)

## 📞 Поддержка

Если возникли вопросы - проверьте логи в консоли backend/frontend.
