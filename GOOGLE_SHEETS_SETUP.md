# Настройка Google Sheets API для парсинга видео

## 📋 Что реализовано

Система парсинга YouTube видео автоматически извлекает:
1. **Таймкоды (chapters)** - разделы видео с временными метками
2. **Транскрипт** - полный текст из автогенерируемых или ручных субтитров  
3. **Метаданные** - название, канал, просмотры, лайки, теги

Все данные сохраняются в **Google Sheets** для удобного анализа.

---

## 🔧 Пошаговая настройка

### Шаг 1: Создать Google Cloud проект

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Название: `YT Zavod Video Parser` (или любое другое)

### Шаг 2: Включить Google Sheets API

1. В меню слева: **APIs & Services** → **Enabled APIs & services**
2. Нажмите **+ ENABLE APIS AND SERVICES**
3. Найдите "Google Sheets API"
4. Нажмите **Enable**

### Шаг 3: Создать Service Account

1. **APIs & Services** → **Credentials**
2. Нажмите **+ CREATE CREDENTIALS** → **Service account**
3. Заполните:
   - **Service account name**: `yt-zavod-parser`
   - **Service account ID**: `yt-zavod-parser` (автозаполнится)
   - **Description**: `Service account for parsing YouTube videos to Google Sheets`
4. Нажмите **CREATE AND CONTINUE**
5. **Role**: Select a role → **Editor** (или **Owner** для полного доступа)
6. Нажмите **CONTINUE** → **DONE**

### Шаг 4: Создать JSON ключ

1. На странице **Credentials** найдите созданный Service Account
2. Нажмите на его email (например, `yt-zavod-parser@...`)
3. Перейдите на вкладку **KEYS**
4. **ADD KEY** → **Create new key**
5. Выберите **JSON**
6. Файл автоматически скачается на компьютер

### Шаг 5: Настроить ключ через переменные окружения (рекомендуется)

Чтобы не хранить секреты в репозитории, используйте один из вариантов:

Вариант A — файл на диске (рекомендовано)
- Оставьте JSON ключ в любом безопасном месте на диске (НЕ в гите)
- В корневом `.env` добавьте:

```properties
# Путь к файлу ключа сервисного аккаунта
GOOGLE_CREDENTIALS_PATH=D:\Projects\YT_combiner\python-workers\google-credentials.json
```

Вариант B — стандарт Google
- Используйте переменную `GOOGLE_APPLICATION_CREDENTIALS`:

```properties
GOOGLE_APPLICATION_CREDENTIALS=D:\path\to\service-account.json
```

Вариант C — без файла (JSON в переменной)
- Можно положить JSON напрямую в переменную (удобно для CI)
- Разрешены raw JSON и base64:

```properties
# raw JSON (одной строкой)
GOOGLE_CREDENTIALS_JSON={"type":"service_account", ...}

# или base64 того же JSON
GOOGLE_CREDENTIALS_JSON=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50IiwgLi4ufQ==
```

Совместимость (устаревающий способ)
- Можно по-прежнему положить ключ рядом со скриптами: `python-workers/google-credentials.json`
- Этот файл добавлен в `.gitignore` и не попадёт в репозиторий

> Примечание: Backend и Python-скрипт автоматически подхватят `.env` из корня. Ничего дополнительно стартовать не нужно — просто перезапустите сервер, если он уже был запущен.

### Шаг 6: Создать Google Sheets документ

1. Откройте [Google Sheets](https://sheets.google.com/)
2. Создайте новую таблицу: **Blank** → Назовите `YT Zavod - Parsed Videos`
3. Скопируйте **Spreadsheet ID** из URL:
   ```
   https://docs.google.com/spreadsheets/d/1a2B3c4D5e6F7g8H9i0J_EXAMPLE/edit
                                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                                          Это Spreadsheet ID
   ```
4. **ВАЖНО**: Дать доступ Service Account:
   - Нажмите **Share** (вверху справа)
   - Вставьте email Service Account (например, `yt-zavod-parser@....iam.gserviceaccount.com`)
   - Выберите **Editor**
   - Снимите галочку "Notify people" 
   - Нажмите **Share**

---

## 🚀 Использование

### Автоматический парсинг из интерфейса

1. Перейдите на страницу **Скачивание** в YT Zavod
2. Скачайте видео или найдите уже скачанное
3. Нажмите кнопку **Парсить** напротив видео
4. Данные автоматически сохранятся в Google Sheets

### Ручной парсинг через Python

```bash
cd python-workers

# Парсинг с сохранением в Google Sheets
python video_parser.py dQw4w9WgXcQ --spreadsheet 1a2B3c4D5e6F7g8H9i0J_EXAMPLE

# Парсинг только с сохранением в JSON
python video_parser.py dQw4w9WgXcQ

# Указать языки для транскрипта
python video_parser.py dQw4w9WgXcQ --languages ru en de
 
# (Опционально) Явный путь к ключу, если не используете .env
python video_parser.py dQw4w9WgXcQ --credentials google-credentials.json --spreadsheet 1a2B3c4D5e6F7g8H9i0J_EXAMPLE
```

### Формат данных в Google Sheets

| Колонка | Описание |
|---------|----------|
| **Video ID** | Уникальный ID видео |
| **Название** | Название видео |
| **Канал** | Имя канала |
| **Длительность** | Секунды |
| **Просмотры** | Количество просмотров |
| **Лайки** | Количество лайков |
| **Кол-во таймкодов** | Количество разделов |
| **Таймкоды (JSON)** | Все таймкоды в JSON формате |
| **Полный текст** | Транскрипт видео (до 50k символов) |
| **Дата загрузки** | Дата публикации видео |
| **Теги** | Теги (до 10 первых) |

---

## 🔍 Примеры таймкодов

Парсер извлекает таймкоды из:
1. **Официальных chapters** YouTube (если автор их добавил)
2. **Описания видео** (автоматический парсинг паттернов вида `0:00 - Intro`)

Формат в JSON:
```json
[
  {
    "start_time": 0,
    "end_time": 45,
    "title": "Intro"
  },
  {
    "start_time": 45,
    "end_time": 180,
    "title": "Main Content"
  }
]
```

---

## 🐍 Установка Python зависимостей

```bash
cd python-workers
pip install -r requirements.txt
```

**requirements.txt** включает:
- `google-api-python-client` - Google Sheets API
- `google-auth-httplib2` - Аутентификация
- `google-auth-oauthlib` - OAuth2
- `yt-dlp` - Скачивание и метаданные видео
- `youtube-transcript-api` - Получение субтитров

---

## ⚠️ Важные замечания

1. **Приватность**: Service Account не может читать ваши личные таблицы. Нужно явно давать доступ через Share
2. **Лимиты Google API**: 
   - Read/Write requests: 300 в минуту на пользователя
   - Read requests: 60 в минуту на пользователя
3. **Размер текста**: Транскрипт ограничен 50,000 символов (лимит Google Sheets для одной ячейки)
4. **Языки субтитров**: Парсер пытается найти субтитры в порядке: ru → en → другие доступные

---

## 🛠️ Troubleshooting

### Ошибка: "Permission denied"
- Проверьте, что Service Account email добавлен в **Share** таблицы
- Роль должна быть **Editor** или выше

### Ошибка: "API not enabled"
- Убедитесь, что Google Sheets API включен в Cloud Console
- Подождите 1-2 минуты после включения API

### Ошибка: "Invalid credentials"
- Если используете файл: проверьте `GOOGLE_CREDENTIALS_PATH` или `GOOGLE_APPLICATION_CREDENTIALS`
- Если используете JSON в переменной: проверьте формат `GOOGLE_CREDENTIALS_JSON` (raw или base64)
- При необходимости укажите `--credentials` явно и проверьте, что файл существует
- Убедитесь, что файл не поврежден (валидный JSON)

### Секреты попали в репозиторий
- Немедленно удалите ключи из истории (BFG/git-filter-repo) и ротируйте ключ в Google Cloud
- В дальнейшем используйте `.env` и держите секреты fuera репозитория

### Субтитры не найдены
- Не у всех видео есть субтитры
- Попробуйте другие языки: `--languages en de fr`
- Некоторые видео блокируют доступ к субтитрам

---

## 📚 Дополнительные ресурсы

- [Google Sheets API Documentation](https://developers.google.com/sheets/api)
- [yt-dlp Documentation](https://github.com/yt-dlp/yt-dlp)
- [youtube-transcript-api](https://github.com/jdepoix/youtube-transcript-api)
