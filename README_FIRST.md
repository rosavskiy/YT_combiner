# ⚡ БЫСТРЫЙ СТАРТ - ЧИТАЙТЕ ЭТО!

## 🎯 Текущая ситуация

✅ **Backend запущен** (или перезапускается)  
⏳ **Frontend нужно запустить**

## 📝 ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС

### Шаг 1: Откройте НОВЫЙ терминал PowerShell

Нажмите `Win + X` → выберите "Windows PowerShell" или "Терминал"

### Шаг 2: Выполните эти команды

```powershell
cd D:\Projects\YT_combiner\frontend
npm install
npm run dev
```

### Шаг 3: Откройте браузер

Перейдите на **http://localhost:5173**

### Шаг 4: Используйте приложение

1. Откройте раздел **"Тренды"** в боковом меню
2. Нажмите **"Обновить тренды"**
3. Наблюдайте за загрузкой трендов из 19 стран! 🌍

---

## ❓ Если что-то не работает

### Backend не отвечает?
```powershell
# Остановите текущий терминал (Ctrl+C)
# Затем запустите заново:
cd D:\Projects\YT_combiner\backend
npm run dev
```

### Ошибка "порт занят"?
Проверьте что запущен только один backend:
```powershell
netstat -ano | findstr :3000
```

### MongoDB ошибки?
Это нормально! Приложение работает без MongoDB.  
Данные просто не сохраняются между перезапусками.

---

## 🎉 Что дальше?

После запуска вы сможете:
- ✅ Получать тренды из 19 стран
- ✅ Фильтровать по странам
- ✅ Видеть статистику видео
- ✅ Наблюдать real-time прогресс

**P.S. Ваш API ключ уже в `.env` файле! 🔑**

---

## 🔑 Google Sheets креды (для сохранения в таблицы)

Чтобы парсер мог писать данные в Google Sheets, укажите сервисный аккаунт через переменные окружения (в `.env`):

```properties
# Рекомендуется: путь к JSON ключу (файл НЕ хранить в гите)
GOOGLE_CREDENTIALS_PATH=D:\Projects\YT_combiner\python-workers\google-credentials.json

# Альтернативы (любая из них):
# GOOGLE_APPLICATION_CREDENTIALS=D:\path\to\service-account.json
# GOOGLE_CREDENTIALS_JSON={"type":"service_account", ...}     # raw JSON
# GOOGLE_CREDENTIALS_JSON=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50Iiwg...  # base64 JSON
```

После сохранения .env перезапустите backend.
