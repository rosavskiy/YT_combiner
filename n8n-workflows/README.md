# n8n Workflows для YT Zavod

Готовые workflow для автоматизации работы с YouTube каналами и видео.

## 📦 Файлы

1. **telegram-add-channel.json** - Добавление каналов через Telegram
2. **video-auto-parse.json** - Автоматический парсинг и уведомления
3. **monitor-channels.json** - Мониторинг новых видео на каналах

## 🚀 Быстрый старт

### 1. Импорт в n8n

1. Откройте ваш n8n сервер
2. Нажмите **"Import from File"** или **"Import from URL"**
3. Загрузите один из JSON файлов
4. Workflow появится в списке

### 2. Настройка переменных окружения

В n8n добавьте следующие переменные:

```bash
BACKEND_URL=https://your-backend-url.com
TELEGRAM_ADMIN_CHAT_ID=your-telegram-chat-id
```

### 3. Настройка Credentials

#### A. YT Zavod API Token
1. Создайте **HTTP Header Auth** credential
2. Header Name: `Authorization`
3. Header Value: `Bearer YOUR_JWT_TOKEN`
4. Сохраните как **"YT Zavod API Token"**

#### B. Telegram Bot
1. Создайте **Telegram** credential
2. Access Token: `YOUR_TELEGRAM_BOT_TOKEN`
3. Сохраните как **"Telegram Bot"**

## 📋 Описание Workflows

### 1️⃣ Telegram Add Channel

**Назначение:** Прием команд от Telegram бота для добавления каналов

**Триггер:** Webhook от Telegram

**Что делает:**
- Принимает команду `/add_channel <URL>`
- Проверяет формат команды
- Пересылает запрос на YT Zavod backend
- Backend обрабатывает добавление канала
- Отправляет подтверждение пользователю

**Настройка:**
1. Активируйте workflow
2. Скопируйте Production Webhook URL (например: `https://n8n.example.com/webhook/telegram-bot`)
3. Установите webhook для Telegram бота:
   ```bash
   curl -X POST https://api.telegram.org/bot<BOT_TOKEN>/setWebhook \
     -d "url=https://n8n.example.com/webhook/telegram-bot"
   ```

**Тестирование:**
```
/add_channel https://youtube.com/@channel
```

---

### 2️⃣ Video Auto Parse

**Назначение:** Получение уведомлений о завершении парсинга видео

**Триггер:** Webhook от YT Zavod backend

**Что делает:**
- Получает событие `video-parsed` от backend
- Проверяет статус (completed/failed)
- При успехе:
  - Извлекает данные о транскрипте
  - Получает полный текст через API
  - Отправляет уведомление в Telegram
- При ошибке:
  - Отправляет уведомление об ошибке

**Настройка:**
1. Активируйте workflow
2. Скопируйте Production Webhook URL (например: `https://n8n.example.com/webhook/video-parsed`)
3. Добавьте в `.env` backend:
   ```bash
   N8N_WEBHOOK_URL=https://n8n.example.com/webhook/video-parsed
   ```

**Структура события:**
```json
{
  "event": "video-parsed",
  "videoId": "dQw4w9WgXcQ",
  "status": "completed",
  "result": {
    "transcript": {...},
    "chapters": [...],
    "full_text": "..."
  },
  "timestamp": "2025-11-17T12:00:00.000Z"
}
```

---

### 3️⃣ Monitor Channels

**Назначение:** Автоматический мониторинг новых видео на отслеживаемых каналах

**Триггер:** Schedule (каждые 6 часов)

**Что делает:**
1. Каждые 6 часов запрашивает активность каналов через API
2. Фильтрует видео, опубликованные за последние 6 часов
3. Для каждого нового видео:
   - Добавляет в очередь скачивания
   - Автоматически запускается парсинг (если `AUTO_PARSE_AFTER_DOWNLOAD=true`)
4. Отправляет сводку в Telegram:
   - Количество новых видео
   - Список с названиями, каналами, ссылками
   - Количество просмотров

**Настройка:**
1. Активируйте workflow
2. Измените интервал проверки при необходимости:
   - Каждый час: `hoursInterval: 1`
   - Каждые 12 часов: `hoursInterval: 12`
   - Раз в день: `hoursInterval: 24`

**Пример уведомления:**
```
🆕 Найдено новых видео: 3

1. Название первого видео
   📺 Название канала
   🔗 https://youtube.com/watch?v=xxx
   👁️ 12,345 просмотров

2. Название второго видео
   📺 Другой канал
   🔗 https://youtube.com/watch?v=yyy
   👁️ 5,678 просмотров

3. Название третьего видео
   📺 Еще канал
   🔗 https://youtube.com/watch?v=zzz
   👁️ 98,765 просмотров
```

---

## 🔧 Дополнительные настройки

### Изменение качества скачивания

В workflow **monitor-channels.json** найдите узел **"Download Video"** и измените:

```json
{
  "quality": "1080p"  // highest, 1080p, 720p, 480p, 360p
}
```

### Изменение языков для парсинга

Backend использует настройку по умолчанию `['en', 'ru']`. Для изменения добавьте в `.env`:

```bash
DEFAULT_TRANSCRIPT_LANGUAGES=en,ru,uk,de,fr
```

### Фильтрация по просмотрам

В workflow **monitor-channels.json** добавьте в **"Filter New Videos"**:

```javascript
const newVideos = activities.filter(v => {
  const publishedDate = new Date(v.publishedAt);
  const sixHoursAgo = new Date(Date.now() - 6 * 60 * 60 * 1000);
  const views = v.views || 0;
  
  // Только видео с более 1000 просмотров
  return publishedDate > sixHoursAgo && views > 1000;
});
```

---

## 🐛 Устранение проблем

### Webhook не срабатывает

1. Проверьте, что workflow активирован (зеленый переключатель)
2. Используйте **Production URL**, а не Test URL
3. Проверьте логи n8n: Settings → Executions
4. Тестируйте вручную через "Execute Workflow"

### Ошибка авторизации API

1. Проверьте JWT токен в credentials
2. Токен должен быть действительным (срок жизни 30 дней)
3. Получите новый токен через авторизацию в веб-интерфейсе
4. Обновите credentials в n8n

### Telegram не отправляет сообщения

1. Проверьте BOT_TOKEN в credentials
2. Получите CHAT_ID: отправьте `/start` боту и проверьте:
   ```bash
   curl https://api.telegram.org/bot<TOKEN>/getUpdates
   ```
3. Убедитесь, что бот имеет права на отправку сообщений

### Backend не получает webhook

1. Проверьте `N8N_WEBHOOK_URL` в `.env`
2. URL должен быть доступен извне (публичный адрес)
3. Проверьте CORS настройки backend
4. Просмотрите логи backend: `npm run dev`

---

## 📚 Дополнительные ресурсы

- [n8n Documentation](https://docs.n8n.io/)
- [YT Zavod API Documentation](../AUTOMATION_GUIDE.md)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

## 💡 Идеи для расширения

### 1. Автоматическая публикация в соцсети
- Добавьте узел для публикации транскрипта в Twitter/VK
- Используйте Google Sheets для планирования публикаций

### 2. Аналитика конкурентов
- Собирайте метрики по каналам (просмотры, лайки, комментарии)
- Сохраняйте в базу данных для анализа трендов
- Стройте графики в Google Data Studio

### 3. Умные уведомления
- Используйте AI для анализа тематики видео
- Отправляйте уведомления только по релевантным темам
- Фильтруйте по ключевым словам в заголовках

### 4. Интеграция с CRM
- Автоматически создавайте задачи в Notion/Trello
- Назначайте ответственных за обработку видео
- Отслеживайте прогресс работы команды

---

**Успешной автоматизации! 🚀**
