# 🤖 Настройка Telegram Бота

## ⚡ Уже есть бот для авторизации?

**Вы можете использовать существующего бота!**

👉 **См. подробную инструкцию:** [`TELEGRAM_EXISTING_BOT.md`](./TELEGRAM_EXISTING_BOT.md)

Не нужно создавать нового бота — используйте тот, через который пользователи авторизуются на сайте.

---

## Шаг 1: Создание бота через BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)

2. Отправьте команду `/newbot`

3. Введите **название** бота (например: `YT Zavod Manager`)

4. Введите **username** бота (должен заканчиваться на `bot`, например: `yt_zavod_bot`)

5. **BotFather выдаст вам токен** — сохраните его! Выглядит примерно так:
   ```
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567890
   ```

## Шаг 2: Настройка команд бота

Отправьте BotFather команду `/setcommands`, выберите вашего бота и отправьте:

```
start - Приветствие и инструкции
help - Список доступных команд
add_channel - Добавить канал в отслеживание
list_channels - Показать все отслеживаемые каналы
remove_channel - Удалить канал из отслеживания
```

## Шаг 3: Добавление токена в .env

Откройте файл `backend/.env` и добавьте:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567890
TELEGRAM_WEBHOOK_URL=https://ваш-домен.com/api/telegram/webhook
```

**Замените:**
- `123456789:ABC...` на ваш реальный токен от BotFather
- `ваш-домен.com` на ваш реальный домен или IP

## Шаг 4: Установка webhook

После запуска сервера выполните:

```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://ваш-домен.com/api/telegram/webhook",
    "allowed_updates": ["message"]
  }'
```

**Или через наш эндпоинт:**

```bash
curl -X POST "http://localhost:3000/api/telegram/set-webhook"
```

## Шаг 5: Проверка webhook

Проверьте статус webhook:

```bash
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo"
```

Должен вернуть:
```json
{
  "ok": true,
  "result": {
    "url": "https://ваш-домен.com/api/telegram/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

## Шаг 6: Тестирование

1. Найдите вашего бота в Telegram по username (например: `@yt_zavod_bot`)

2. Нажмите **START** или отправьте `/start`

3. Попробуйте команды:
   ```
   /help
   /list_channels
   /add_channel https://www.youtube.com/@channel_name
   ```

## Доступные команды

### `/start`
Приветственное сообщение с инструкцией

### `/help`
Список всех команд

### `/add_channel <URL>`
Добавить YouTube канал в отслеживание

Примеры:
```
/add_channel https://www.youtube.com/@channelname
/add_channel https://www.youtube.com/c/ChannelName
/add_channel https://www.youtube.com/channel/UCxxxxxxxxx
```

### `/list_channels`
Показать все каналы, которые отслеживает пользователь

### `/remove_channel <channel_id>`
Удалить канал из отслеживания по ID

Пример:
```
/remove_channel 123
```

## Требования для пользователей

⚠️ **Важно:** Пользователь должен быть авторизован в системе через Telegram!

1. Пользователь должен сначала авторизоваться на сайте через Telegram Login Widget
2. Только после этого он сможет использовать команды бота
3. Система проверяет `telegram_id` пользователя

## Безопасность

- ✅ Все запросы проверяются на подлинность через Telegram API
- ✅ Только авторизованные пользователи могут использовать команды
- ✅ Каждый пользователь видит только свои каналы
- ✅ Webhook использует HTTPS (в продакшене)

## Troubleshooting

### Бот не отвечает

1. Проверьте, что сервер запущен:
   ```bash
   pm2 status
   ```

2. Проверьте логи:
   ```bash
   pm2 logs yt-combiner-backend
   ```

3. Проверьте webhook:
   ```bash
   curl "https://api.telegram.org/bot<ТОКЕН>/getWebhookInfo"
   ```

### Ошибка "User not found"

Пользователь должен сначала авторизоваться на сайте через Telegram Login Widget.

### Webhook не устанавливается

- Убедитесь, что используете HTTPS (в продакшене)
- Проверьте, что порт 3000 (или ваш) доступен извне
- Убедитесь, что Nginx правильно проксирует запросы

### Ошибка "Invalid token"

Проверьте токен в `.env` файле — он должен быть без пробелов и кавычек.

## Nginx конфигурация

Убедитесь, что в `nginx.conf` есть проксирование для Telegram webhook:

```nginx
location /api/telegram/ {
    proxy_pass http://localhost:3000/api/telegram/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

## Альтернатива: Polling вместо Webhook

Если webhook не работает (например, на локальной разработке), можно использовать polling. Создайте файл `backend/src/services/telegramPolling.js`:

```javascript
import TelegramBot from 'node-telegram-bot-api';

const token = process.env.TELEGRAM_BOT_TOKEN;
const bot = new TelegramBot(token, { polling: true });

bot.on('message', async (msg) => {
  // Обработка сообщений
  console.log('Получено сообщение:', msg);
});

export default bot;
```

Но для продакшена **рекомендуется использовать webhook**, так как он более надежный и не нагружает сервер.

## Полезные ссылки

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [BotFather Commands](https://core.telegram.org/bots#6-botfather)
- [Webhook Guide](https://core.telegram.org/bots/webhooks)
