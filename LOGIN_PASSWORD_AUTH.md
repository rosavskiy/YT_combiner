# 🔐 Авторизация по логину и паролю - Быстрый старт

## ✅ Что добавлено

Система теперь поддерживает **два способа авторизации**:
1. 🔑 **Логин и пароль** (новое)
2. 📱 **Telegram** (осталось)

---

## 🚀 Создание администратора

### Способ 1: Запустить скрипт (рекомендуется)

```powershell
cd backend
node scripts/create-admin.js
```

Будет создан администратор:
- **Логин**: `rosavsky`
- **Пароль**: `O7gheo13@!`
- **Роль**: admin (подтвержден автоматически)

### Способ 2: Удалить БД и перезапустить (сбросить всё)

```powershell
# Остановите backend (Ctrl+C)
Remove-Item backend\data\trends.db
# Запустите backend снова
cd backend
npm run dev
```

Затем запустите скрипт создания администратора.

---

## 🎯 Использование

### 1. Запустите систему

```powershell
# Терминал 1 - Backend
cd backend
npm run dev

# Терминал 2 - Frontend
cd frontend
npm run dev
```

### 2. Откройте браузер

http://localhost:5173

### 3. Войдите

Вы увидите две вкладки:

#### Вкладка "Логин и пароль" (по умолчанию)
- Логин: `rosavsky`
- Пароль: `O7gheo13@!`
- Нажмите "Войти"

#### Вкладка "Telegram"
- Работает как раньше
- Требует настройки Telegram бота

---

## 👥 Добавление пользователей

### Администратор может:

1. Создать пользователя с логином/паролем через API:
```javascript
POST /api/auth/register
{
  "login": "employee1",
  "password": "SecurePass123!",
  "first_name": "Иван",
  "last_name": "Петров"
}
```

2. Пользователи могут войти через Telegram (требует подтверждения)

---

## 🔐 Безопасность

✅ **Пароли хешируются** с помощью bcrypt (10 раундов)  
✅ **JWT токены** для сессий (30 дней)  
✅ **Rate Limiting** - 10 попыток за 15 минут  
✅ **Оба метода равнозначны** - одинаковая безопасность  

---

## 📊 Структура БД

Таблица `users` теперь имеет:
- `login` - для авторизации по логину/паролю (UNIQUE)
- `password_hash` - хешированный пароль
- `telegram_id` - для авторизации через Telegram (UNIQUE)

Пользователь может иметь:
- Только логин/пароль
- Только Telegram
- Оба метода одновременно

---

## 🛠️ API Endpoints

### POST /api/auth/login
Авторизация по логину и паролю

**Request:**
```json
{
  "login": "rosavsky",
  "password": "O7gheo13@!"
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "user": { ... },
    "token": "JWT_TOKEN",
    "requiresApproval": false
  }
}
```

### POST /api/auth/telegram
Авторизация через Telegram (работает как раньше)

---

## 📝 Примечания

### Первый администратор

Скрипт `create-admin.js`:
- ✅ Создает пользователя с логином `rosavsky`
- ✅ Устанавливает пароль `O7gheo13@!`
- ✅ Назначает роль `admin`
- ✅ Автоматически подтверждает (is_approved = 1)
- ⚠️ Если пользователь существует - выводит сообщение

### Смена пароля

В будущем можно добавить эндпоинт для смены пароля:
```javascript
POST /api/auth/change-password
{
  "oldPassword": "старый",
  "newPassword": "новый"
}
```

---

## ✅ Готово!

Теперь у вас есть два способа входа:
1. 🔑 **Логин/пароль** - для администратора и внутренних пользователей
2. 📱 **Telegram** - для удобного входа сотрудников

**Рекомендация**: Администратор использует логин/пароль, сотрудники - Telegram.

**Удачи! 🚀**
