# ⚡ Быстрый старт с SQLite

## ✅ Что готово

База данных **SQLite** уже настроена и работает! Никаких дополнительных действий не требуется.

## 📦 Что такое SQLite?

SQLite — это **файловая база данных**. Это означает:
- ❌ Не нужно устанавливать MongoDB, PostgreSQL или другие серверы БД
- ❌ Не нужно запускать дополнительные процессы
- ❌ Не нужно настраивать порты и аутентификацию
- ✅ Просто запускаете приложение — база данных работает

## 🗄️ Где хранятся данные?

```
backend/data/
├── trends.db       ← Основной файл базы данных
├── trends.db-shm   ← Служебный (shared memory)
└── trends.db-wal   ← Служебный (write-ahead log)
```

## 🚀 Запуск

```bash
# 1. Backend с SQLite (запустится автоматически)
cd backend
npm run dev

# 2. Frontend
cd frontend
npm run dev
```

Вы увидите:
```
✅ SQLite база данных инициализирована
📁 Путь к БД: D:\Projects\YT_combiner\backend\data\trends.db
```

## 📊 Как работает

### 1. Загрузка трендов
Когда вы нажимаете "Обновить тренды":
1. Данные загружаются из YouTube API
2. **Автоматически сохраняются в SQLite**
3. Отображаются в таблице

### 2. Просмотр истории
```javascript
GET /api/trends/latest   // Последние тренды
GET /api/trends/history  // История загрузок
GET /api/trends/:id      // Конкретная загрузка
```

### 3. Сохранность данных
- ✅ Данные сохраняются на диск
- ✅ Остаются после перезагрузки сервера
- ✅ Можно копировать файл `trends.db` для резервной копии

## 🔍 Проверка данных

### Через SQLite CLI (если установлен)
```bash
sqlite3 backend/data/trends.db

# Показать таблицы
.tables

# Количество трендов
SELECT COUNT(*) FROM trends;

# Последние 5 загрузок
SELECT id, created_at, total_videos FROM trends 
ORDER BY created_at DESC LIMIT 5;

# Выход
.quit
```

### Через Node.js
```javascript
import Database from 'better-sqlite3';
const db = new Database('./backend/data/trends.db');

// Все тренды
const trends = db.prepare('SELECT * FROM trends').all();
console.log(trends);
```

## 💡 Преимущества для разработки

### До (MongoDB):
```bash
# Нужно было:
1. Установить MongoDB Server
2. Запустить mongod
3. Настроить connection string
4. Проверить подключение
5. Только потом запускать приложение
```

### Сейчас (SQLite):
```bash
# Нужно:
1. npm run dev
```

## 🎯 Миграция данных

Если у вас была MongoDB и есть данные:

```javascript
// export-mongo.js
import mongoose from 'mongoose';
import Trend from './models/Trend.js';
import fs from 'fs';

await mongoose.connect('mongodb://localhost/ytcombiner');
const trends = await Trend.find().lean();
fs.writeFileSync('trends-export.json', JSON.stringify(trends));
```

```javascript
// import-sqlite.js
import TrendModel from './models/TrendSQLite.js';
import fs from 'fs';

const trends = JSON.parse(fs.readFileSync('trends-export.json'));
for (const trend of trends) {
  TrendModel.create({
    data: trend.data,
    totalVideos: trend.totalVideos,
    countries: trend.countries
  });
}
```

## 🔧 Настройки (опционально)

Файл `backend/src/config/sqlite.js`:

```javascript
// Изменить путь к БД
const dbPath = join(process.cwd(), 'data', 'trends.db');

// Включить/выключить WAL mode
db.pragma('journal_mode = WAL'); // Для production
db.pragma('journal_mode = DELETE'); // Для простоты

// Настроить cache
db.pragma('cache_size = -64000'); // 64MB
```

## ❓ FAQ

**Q: Где будут храниться видео файлы?**  
A: SQLite хранит только метаданные. Видео файлы в `backend/downloads/`.

**Q: Можно ли использовать для production?**  
A: Да! SQLite используют:
- Все браузеры (Chrome, Firefox, Safari)
- Apple iOS и macOS
- Android
- Многие embedded системы

**Q: Что если нужно масштабировать?**  
A: Для MVP (1-10K запросов/день) SQLite более чем достаточно.  
Для роста можно мигрировать на PostgreSQL позже.

**Q: Как сделать бэкап?**  
A: Просто скопируйте файл `trends.db`:
```bash
cp backend/data/trends.db backend/data/backup-$(date +%Y%m%d).db
```

## 🎉 Готово!

Теперь у вас:
- ✅ Работающая база данных
- ✅ Нулевая конфигурация
- ✅ Автоматическое сохранение
- ✅ Персистентное хранение

Просто запускайте `npm run dev` и работайте! 🚀
