# 🎬 Исследование: Ребилд видео с AI

**Дата:** 14 декабря 2025  
**Задача:** Взять "залетевшее" видео → создать похожее

---

## 📊 Текущее состояние проекта

### ✅ Что уже есть:

1. **Парсинг видео** ✅
   - Загрузка с YouTube (yt-dlp)
   - Извлечение метаданных (название, описание, теги)
   - Таймкоды/главы (chapters)
   - Транскрипт (субтитры + Whisper AI)
   - Полный текст расшифровки (full_text)
   - Сохранение в JSON + Google Sheets

2. **AI Tasks система** ✅
   - Таблица `ai_tasks` в SQLite
   - Bull Queue для асинхронной обработки
   - Хранение промптов, статусов, результатов
   - Привязка к пользователям (owner_user_id)
   - Интеграция с Google Sheets (обновление статуса)

3. **Формирование промпта** ✅
   - Функция `buildPromptFromRow(row)` в GeneratorPage.jsx
   - Собирает: название, описание, теги, длительность
   - Ручное редактирование промпта в UI
   - Шаблон: "Создай короткое видео на основе темы..."

4. **Stub генерация** ✅
   - Простое видео через ffmpeg (черный экран)
   - Используется для тестирования пайплайна
   - Сохранение в `backend/data/ai-outputs/`
   - Статусы: pending → active → completed/failed

### ❌ Чего НЕТ:

- Реальная генерация видео по промпту
- Интеграция с AI-провайдерами видео
- Обработка исходных кадров видео
- Озвучка (TTS)
- Монтаж/склейка сцен
- Добавление субтитров/эффектов

---

## 🎯 Цель: "Ребилд залетевшего видео"

### Что такое "ребилд"?

1. **Входные данные:**
   - Оригинальное видео (скачанное через yt-dlp)
   - Метаданные (название, описание, теги)
   - Транскрипт (полный текст + тайминги)
   - Таймкоды (структура контента)

2. **Выходной результат:**
   - Новое видео с похожим контентом
   - Аналогичная структура (главы/сцены)
   - Новая визуализация (избежать копирайта)
   - Новая озвучка (синтезированный голос)
   - Похожая длительность и темп

3. **Требования:**
   - Уникальность (избежать Content ID match)
   - Качество не хуже оригинала
   - Автоматизация процесса
   - Разумная стоимость

---

## 🔍 Анализ рынка AI-видео провайдеров

### 1. **Runway Gen-3 Alpha** (Text/Image-to-Video)

**Возможности:**
- Генерация 5-10 сек клипов из текста/изображения
- Высокое качество (1080p)
- Стилизация, motion control
- API доступен

**Ограничения:**
- Максимум 10 секунд за раз
- Дорого: $0.05/сек = $3/мин видео
- Нужна склейка множества клипов для длинного видео

**Применимость к ребилду:**
- ⚠️ Подходит для коротких роликов (<60 сек)
- ❌ Неэффективно для длинных видео (10+ мин)
- ✅ Можно использовать для B-roll вставок

**API:** https://docs.runwayml.com/

---

### 2. **Pika Labs** (Text-to-Video)

**Возможности:**
- 3-секундные клипы из промпта
- Расширение видео (extend)
- Редактирование регионов
- Beta API (ограниченный доступ)

**Ограничения:**
- Очень короткие клипы (3 сек)
- Нестабильное качество
- API в закрытом бета-тесте

**Применимость:**
- ❌ Слишком короткие клипы
- ❌ Нет публичного API

---

### 3. **Synthesia** (AI Avatar Video)

**Возможности:**
- AI-аватары (140+ готовых)
- Озвучка на 120+ языках
- Текст → Видео с говорящей головой
- API доступен
- Автоматические субтитры

**Ограничения:**
- Только формат "говорящая голова"
- Дорого: от $30/мес (Personal), API от $1000/мес
- Не подходит для динамичных роликов

**Применимость к ребилду:**
- ✅ Идеально для обучающего контента, презентаций
- ❌ Не подходит для развлекательных/новостных роликов с B-roll
- ✅ Отличная озвучка + синхронизация губ

**API:** https://docs.synthesia.io/

---

### 4. **D-ID** (AI Talking Head)

**Возможности:**
- Фото + текст → говорящая голова
- 100+ голосов
- API с pay-as-you-go
- Быстрая генерация (1-2 мин)

**Ограничения:**
- Только "говорящая голова"
- $0.10/20 сек видео
- Качество лиц иногда uncanny valley

**Применимость:**
- ✅ Дешевле Synthesia
- ❌ Тот же формат (только аватары)

**API:** https://docs.d-id.com/

---

### 5. **HeyGen** (AI Video Generation)

**Возможности:**
- AI аватары (100+)
- Персонализированные аватары ($50-300)
- Мультиязычная озвучка
- API доступен
- Автоматический перевод видео

**Ограничения:**
- От $30/мес (Creator), API от $500/мес
- Опять же формат "говорящая голова"

**Применимость:**
- ✅ Хорошая озвучка
- ❌ Не подходит для динамичного контента

**API:** https://docs.heygen.com/

---

### 6. **Pictory AI** (Long-form Video from Text)

**Возможности:**
- Скрипт/статья → видео
- Автоматический подбор B-roll из библиотеки
- Автоматические субтитры
- Озвучка (AI voices)
- Редактирование существующих видео

**Ограничения:**
- Использует stock footage (риск повторов)
- Нет полноценного API (только webhooks)
- От $23/мес (Standard)

**Применимость к ребилду:**
- ✅ Подходит для новостных/обучающих роликов
- ✅ Работает с длинными видео
- ⚠️ Качество зависит от stock библиотеки
- ❌ Нет программного API

**Сайт:** https://pictory.ai/

---

### 7. **Lumen5** (Social Media Video)

**Возможности:**
- Текст/блог → видео
- Автоматическая нарезка на сцены
- Библиотека медиа (Unsplash, Pexels)
- AI подбор визуалов под текст

**Ограничения:**
- Ориентирован на соцсети (короткие ролики)
- Нет API
- От $29/мес

**Применимость:**
- ❌ Нет API
- ⚠️ Только короткие ролики

---

### 8. **InVideo AI** (Text-to-Video)

**Возможности:**
- Промпт → готовое видео
- Автоматический подбор B-roll
- Озвучка, музыка, субтитры
- Редактирование через команды

**Ограничения:**
- Нет публичного API
- От $25/мес (Plus)

**Применимость:**
- ❌ Нет API (только веб-интерфейс)

---

### 9. **Rephrase.ai** (Personalized Video)

**Возможности:**
- AI-аватары для персонализированных сообщений
- API доступен
- Интеграция с CRM

**Применимость:**
- ❌ Узкая ниша (персонализация для маркетинга)

---

### 10. **ElevenLabs** (AI Voice/TTS) + DIY Монтаж

**Возможности:**
- Лучшая AI озвучка на рынке
- 29 языков
- Клонирование голоса
- API доступен
- $5/мес (Starter), $1/мес (API)

**Применимость к ребилду:**
- ✅ Идеально для озвучки
- ✅ Доступная цена
- ⚠️ Нужна своя визуальная часть

**API:** https://elevenlabs.io/docs/

---

## 🛠️ Самостоятельная реализация (DIY)

### Архитектура "Сборочного конвейера":

```
1. Парсинг оригинала
   ├─> Транскрипт (текст + тайминги)
   ├─> Таймкоды (структура)
   └─> Метаданные

2. Генерация озвучки
   ├─> ElevenLabs API (текст → аудио)
   ├─> Сохранение MP3
   └─> Синхронизация с таймингами

3. Генерация визуала
   ├─> Вариант A: Stock footage (Pexels API)
   ├─> Вариант B: AI-генерация кадров (Stable Diffusion)
   ├─> Вариант C: Скриншоты + эффекты (ffmpeg)
   └─> Подбор по ключевым словам из текста

4. Монтаж (ffmpeg)
   ├─> Склейка видео-сцен
   ├─> Наложение аудио
   ├─> Добавление субтитров (SRT)
   ├─> Переходы между сценами
   └─> Выходной MP4

5. Постобработка
   ├─> Добавление интро/аутро
   ├─> Музыкальный фон
   └─> Цветокоррекция
```

### Необходимые компоненты:

#### A. Озвучка (ElevenLabs)
```javascript
// backend/src/services/ttsService.js
import { ElevenLabsClient } from 'elevenlabs';

class TTSService {
  async textToSpeech(text, voiceId = 'default') {
    const client = new ElevenLabsClient({ apiKey: process.env.ELEVENLABS_API_KEY });
    const audio = await client.generate({
      text,
      voice: voiceId,
      model_id: 'eleven_multilingual_v2'
    });
    return audio; // Buffer
  }
}
```

**Стоимость:** ~$0.30/1000 символов (10 мин видео ≈ 1500 слов ≈ $0.45)

---

#### B. Подбор B-roll (Pexels API — бесплатно!)

```javascript
// backend/src/services/stockFootageService.js
import axios from 'axios';

class StockFootageService {
  async searchVideos(query, perPage = 5) {
    const res = await axios.get('https://api.pexels.com/videos/search', {
      headers: { Authorization: process.env.PEXELS_API_KEY },
      params: { query, per_page: perPage }
    });
    return res.data.videos.map(v => ({
      url: v.video_files.find(f => f.quality === 'hd')?.link,
      duration: v.duration,
      id: v.id
    }));
  }
}
```

**Стоимость:** Бесплатно (лимит 200 req/час)

**Альтернативы:**
- Pixabay API (бесплатно)
- Unsplash (только фото, бесплатно)

---

#### C. Монтаж (ffmpeg + fluent-ffmpeg)

```javascript
// backend/src/services/videoEditorService.js
import ffmpeg from 'fluent-ffmpeg';

class VideoEditorService {
  async mergeClips(clips, audioPath, outputPath) {
    return new Promise((resolve, reject) => {
      const cmd = ffmpeg();
      
      // Добавляем видео-клипы
      clips.forEach(clip => cmd.input(clip.path));
      
      // Добавляем озвучку
      cmd.input(audioPath);
      
      // Склеиваем
      cmd.complexFilter([
        `concat=n=${clips.length}:v=1:a=0 [v]`,
        `[${clips.length}:a]` // Озвучка как последний input
      ])
      .map('[v]')
      .audioCodec('aac')
      .videoCodec('libx264')
      .outputOptions(['-pix_fmt yuv420p', '-movflags +faststart'])
      .output(outputPath)
      .on('end', resolve)
      .on('error', reject)
      .run();
    });
  }
}
```

---

#### D. Субтитры (субпроцесс или библиотека)

```javascript
// backend/src/services/subtitlesService.js
class SubtitlesService {
  generateSRT(segments) {
    return segments.map((seg, i) => {
      const start = this.formatTime(seg.start);
      const end = this.formatTime(seg.start + seg.duration);
      return `${i + 1}\n${start} --> ${end}\n${seg.text}\n`;
    }).join('\n');
  }
  
  formatTime(seconds) {
    const h = Math.floor(seconds / 3600).toString().padStart(2, '0');
    const m = Math.floor((seconds % 3600) / 60).toString().padStart(2, '0');
    const s = Math.floor(seconds % 60).toString().padStart(2, '0');
    const ms = Math.floor((seconds % 1) * 1000).toString().padStart(3, '0');
    return `${h}:${m}:${s},${ms}`;
  }
  
  async burnSubtitles(videoPath, srtPath, outputPath) {
    return new Promise((resolve, reject) => {
      ffmpeg(videoPath)
        .outputOptions([`-vf subtitles=${srtPath}`])
        .output(outputPath)
        .on('end', resolve)
        .on('error', reject)
        .run();
    });
  }
}
```

---

## 💰 Сравнение стоимости

### Сценарий: Ребилд 10-минутного видео (новостной формат)

| Компонент | Провайдер | Стоимость |
|-----------|-----------|-----------|
| **Озвучка** | ElevenLabs | $0.45 (1500 слов) |
| **B-roll (10 клипов)** | Pexels API | $0 (бесплатно) |
| **Монтаж** | ffmpeg (локально) | $0 |
| **Субтитры** | ffmpeg (локально) | $0 |
| **ИТОГО** | | **$0.45/видео** |

### Альтернатива: Готовые сервисы

| Сервис | Стоимость 10-мин видео | Качество | API |
|--------|----------------------|----------|-----|
| Synthesia | ~$100 (оценочно) | ⭐⭐⭐⭐⭐ | ✅ |
| Pictory | $23/мес (лимиты) | ⭐⭐⭐⭐ | ❌ |
| Runway Gen-3 | $30 (600 сек × $0.05) | ⭐⭐⭐⭐⭐ | ✅ |
| DIY (наш подход) | $0.45 | ⭐⭐⭐⭐ | ✅ |

---

## 🎯 Рекомендация: Гибридный подход

### Для новостных/обучающих роликов (5-15 мин):

```
1. Парсинг → Получаем транскрипт
2. ElevenLabs → Озвучка ($0.45/видео)
3. Pexels API → Подбор B-roll (бесплатно)
4. ffmpeg → Монтаж (локально)
5. ffmpeg → Субтитры (локально)
```

**Преимущества:**
- ✅ Дешево ($0.45/видео)
- ✅ Полный контроль
- ✅ Масштабируемо
- ✅ Уникальность (избегаем Content ID)

**Недостатки:**
- ⚠️ Требуется разработка
- ⚠️ Качество зависит от stock footage
- ⚠️ Нужен мощный сервер для ffmpeg

---

### Для коротких вирусных роликов (<60 сек):

```
1. Парсинг → Получаем транскрипт
2. Runway Gen-3 → Генерация визуала ($3/мин)
3. ElevenLabs → Озвучка ($0.15)
4. ffmpeg → Наложение аудио
```

**Стоимость:** $3.15/ролик (для 60 сек)

---

### Для аватарных презентаций:

```
1. Парсинг → Транскрипт
2. Synthesia API → Полное видео
```

**Стоимость:** Зависит от тарифа (API от $1000/мес)

---

## 📋 План реализации (DIY подход)

### Этап 1: Инфраструктура (1-2 дня)

- [ ] Установить `fluent-ffmpeg`, `elevenlabs`
- [ ] Создать `backend/src/services/ttsService.js`
- [ ] Создать `backend/src/services/stockFootageService.js`
- [ ] Создать `backend/src/services/videoEditorService.js`
- [ ] Создать `backend/src/services/subtitlesService.js`
- [ ] Добавить env переменные: `ELEVENLABS_API_KEY`, `PEXELS_API_KEY`

### Этап 2: Пайплайн ребилда (2-3 дня)

- [ ] Создать `backend/src/services/videoRebuildService.js`
- [ ] Реализовать метод `rebuildVideo(parsedData, options)`
  - Разбить транскрипт на сегменты (по главам или каждые 30 сек)
  - Для каждого сегмента:
    - Извлечь ключевые слова (keyword extraction)
    - Найти B-roll через Pexels API
    - Скачать клип
  - Сгенерировать озвучку через ElevenLabs
  - Склеить всё через ffmpeg
  - Добавить субтитры
- [ ] Сохранить результат в `backend/data/ai-outputs/rebuilt/`

### Этап 3: Интеграция с AI Tasks (1 день)

- [ ] Обновить `aiVideoService.js`:
  - Добавить provider: `'rebuild'`
  - В обработчике вызывать `videoRebuildService.rebuildVideo()`
- [ ] Обновить фронтенд `GeneratorPage.jsx`:
  - Добавить опцию "Rebuild видео"
  - При выборе передавать `video_id` оригинала
- [ ] Обновить промпт:
  - Добавить поле "Original Video ID"
  - Автоматически подтягивать данные из `videos` таблицы

### Этап 4: UI/UX (1 день)

- [ ] Страница "Rebuild"
  - Выбор видео из списка скачанных
  - Настройки: голос, стиль, длительность
  - Превью транскрипта
  - Кнопка "Запустить ребилд"
- [ ] Мониторинг прогресса
  - Статус: "Парсинг", "Озвучка", "Подбор B-roll", "Монтаж"
  - Прогресс-бар
- [ ] Превью результата
  - Плеер для просмотра
  - Скачивание MP4

### Этап 5: Оптимизация (1-2 дня)

- [ ] Кэширование B-roll (избегать повторных загрузок)
- [ ] Параллельная обработка сегментов
- [ ] Очередь задач (несколько ребилдов одновременно)
- [ ] Логирование и алерты

---

## 🚀 Быстрый старт (MVP за 1 день)

### Минимальная реализация:

```javascript
// backend/src/services/videoRebuildService.js
import { ElevenLabsClient } from 'elevenlabs';
import ffmpeg from 'fluent-ffmpeg';
import fs from 'fs';
import path from 'path';

class VideoRebuildService {
  async rebuildSimple(videoId) {
    // 1. Получить parsed данные
    const parsedPath = path.join(__dirname, '../../../python-workers', `${videoId}_parsed.json`);
    const data = JSON.parse(fs.readFileSync(parsedPath, 'utf-8'));
    
    // 2. Озвучить транскрипт
    const elevenlabs = new ElevenLabsClient({ apiKey: process.env.ELEVENLABS_API_KEY });
    const audio = await elevenlabs.generate({
      text: data.full_text.substring(0, 5000), // Лимит для теста
      voice: 'default',
      model_id: 'eleven_multilingual_v2'
    });
    
    const audioPath = path.join(__dirname, '../../data/ai-outputs', `${videoId}_voice.mp3`);
    fs.writeFileSync(audioPath, audio);
    
    // 3. Создать простое видео (черный экран + озвучка)
    const outputPath = path.join(__dirname, '../../data/ai-outputs', `${videoId}_rebuilt.mp4`);
    
    await new Promise((resolve, reject) => {
      ffmpeg()
        .input('color=c=black:s=1280x720:d=30')
        .inputFormat('lavfi')
        .input(audioPath)
        .outputOptions(['-c:v libx264', '-c:a aac', '-shortest'])
        .output(outputPath)
        .on('end', resolve)
        .on('error', reject)
        .run();
    });
    
    return outputPath;
  }
}

export default new VideoRebuildService();
```

### Тестирование:

```bash
# 1. Установить зависимости
npm install elevenlabs fluent-ffmpeg

# 2. Добавить в .env
ELEVENLABS_API_KEY=sk_xxxxx

# 3. Запустить тест
node -e "import('./backend/src/services/videoRebuildService.js').then(m => m.default.rebuildSimple('VIDEO_ID'))"
```

---

## 📊 Итоговая рекомендация

### ✅ Лучший подход для вашего проекта:

**Гибридный DIY с использованием готовых API**

```
┌─────────────────────────────────────────────┐
│  1. Парсинг (уже есть)                      │
│     └─> full_text, segments, chapters       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  2. Сегментация контента                    │
│     └─> Разбить на сцены по главам          │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  3. Озвучка (ElevenLabs API)                │
│     └─> Текст → MP3 ($0.45/10 мин)          │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  4. Подбор визуала (Pexels API)             │
│     └─> Ключевые слова → B-roll (бесплатно) │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  5. Монтаж (ffmpeg локально)                │
│     └─> Склейка + субтитры                  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  6. Готовое видео в ai-outputs/             │
└─────────────────────────────────────────────┘
```

**Стоимость:** $0.45/видео (только озвучка)  
**Время разработки:** 5-7 дней  
**Качество:** ⭐⭐⭐⭐ (хорошее для новостей/обучения)  
**Масштабируемость:** ✅ Отлично

---

## 🎬 Следующие шаги

1. **Обсудить с вами:**
   - Какой формат видео планируется (новости/обучение/развлечение)?
   - Какой бюджет на озвучку?
   - Нужны ли AI-аватары или только B-roll?

2. **Создать техническое задание:**
   - Детальная архитектура пайплайна
   - API endpoints
   - Структура БД (дополнительные поля)

3. **Начать разработку:**
   - Этап 1: TTS интеграция (ElevenLabs)
   - Этап 2: Stock footage интеграция (Pexels)
   - Этап 3: Монтаж (ffmpeg)
   - Этап 4: UI для управления

**Готовы начинать? Какой формат видео вас интересует больше всего?** 🚀
