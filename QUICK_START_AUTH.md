# 🚀 Быстрый старт - Авторизация через Telegram

## ⚡ 3 шага до запуска

### 1️⃣ Создайте Telegram бота (5 минут)

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду: `/newbot`
3. Назовите бота: `YT Zavod Auth Bot`
4. Придумайте username: `yt_zavod_auth_bot` (должен заканчиваться на `bot`)
5. **Сохраните токен!** Выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
6. Настройте домен: `/setdomain` → выберите бота → укажите `http://localhost:5173`

---

### 2️⃣ Настройте переменные окружения

#### Backend: `.env` (в корне проекта)

Скопируйте `.env.example` в `.env`:

```powershell
Copy-Item .env.example .env
```

Откройте `.env` и заполните:

```bash
# ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ ЭТИ ЗНАЧЕНИЯ!
JWT_SECRET=ваш_очень_длинный_секретный_ключ_минимум_64_символа_смените_меня
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BOT_USERNAME=yt_zavod_auth_bot

# Остальное можно оставить как есть
YOUTUBE_API_KEY=your_youtube_api_key_here
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
```

**Генерация JWT_SECRET:**
```powershell
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 64 | ForEach-Object {[char]$_})
```

#### Frontend: `frontend/.env`

Скопируйте пример:

```powershell
Copy-Item frontend\.env.example frontend\.env
```

Откройте `frontend/.env` и заполните:

```bash
VITE_API_URL=http://localhost:3000
VITE_TELEGRAM_BOT_USERNAME=yt_zavod_auth_bot
```

---

### 3️⃣ Запустите систему

Откройте **2 терминала PowerShell**:

#### Терминал 1: Backend

```powershell
cd backend
npm install  # если еще не установлены зависимости
npm run dev
```

Должны увидеть:
```
✅ SQLite база данных инициализирована
🚀 YT Zavod Backend
📡 Сервер запущен: http://localhost:3000
```

#### Терминал 2: Frontend

```powershell
cd frontend
npm install  # если еще не установлены зависимости
npm run dev
```

Должны увидеть:
```
VITE ready in 500 ms
➜ Local: http://localhost:5173/
```

---

## ✅ Готово! Проверка работы

### Первый вход (вы станете администратором!)

1. Откройте в браузере: **http://localhost:5173**
2. Вы увидите страницу с кнопкой "Login with Telegram"
3. Нажмите на кнопку
4. Telegram откроется для подтверждения
5. Подтвердите вход
6. 🎉 **Вы вошли как администратор!**

### Проверьте, что всё работает:

- ✅ В левом меню должен появиться пункт "Пользователи" (👥)
- ✅ Внизу сайдбара - ваше имя и аватар из Telegram
- ✅ Можете зайти в "Пользователи" и увидеть себя в списке

---

## 👥 Добавление сотрудников

### Новый сотрудник:

1. Заходит на сайт
2. Нажимает "Login with Telegram"
3. Видит: **"Ожидание подтверждения администратором"**

### Вы (администратор):

1. Переходите в раздел **"Пользователи"** (👥)
2. Видите нового сотрудника в разделе **"Ожидают подтверждения"**
3. Нажимаете **"Подтвердить" ✅**
4. Сотрудник получает доступ!

---

## 🎯 Что дальше?

- 📖 Полная инструкция: `TELEGRAM_AUTH_SETUP.md`
- 📋 Описание системы: `AUTH_SYSTEM_COMPLETE.md`
- 🔧 Настройка для продакшена - смотрите чек-лист в `AUTH_SYSTEM_COMPLETE.md`

---

## 🐛 Проблемы?

### Кнопка Telegram не появляется?
➡️ Проверьте `VITE_TELEGRAM_BOT_USERNAME` в `frontend/.env`

### Ошибка "TELEGRAM_BOT_TOKEN не настроен"?
➡️ Проверьте `.env` в корне проекта (не в backend!)

### "Данные не прошли проверку"?
➡️ Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный

### Подробнее в TELEGRAM_AUTH_SETUP.md

---

## 🎉 Готово!

Теперь ваша система защищена авторизацией через Telegram с подтверждением администратором!

**Первый вход = вы администратор**  
**Новые пользователи = требуют вашего подтверждения**

**Удачи! 🚀**
